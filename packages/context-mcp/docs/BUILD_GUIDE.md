# Build guide

Run `npm install`, `npm run check:types`, and `npm run build` from this package.
The server compiles with TypeScript; compilation errors fail the build.
See [the package README](../README.md) for tools and migration details.
