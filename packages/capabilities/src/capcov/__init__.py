"""capcov -- derive what a system can do, map each thing to a test, and fail the
build when the two drift apart in either direction.

The standalone engine is shared by consuming repositories; models and runtime
recipes belong to those consumers.
"""

__version__ = "0.1.0"
