"""FODA feature models for capcov: capability decomposition beside flow reconciliation.

``model``    the feature-model shape, its validator, configuration validity, and a
             binary coverage roll-up (``coverage_rollup``).
``coverage`` the numeric completeness vector (``rollup``) -- coverage that never
             collapses to a single percentage.
``cli``      the ``capcov features`` verbs.
"""
